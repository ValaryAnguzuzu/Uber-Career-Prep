Class ListNode: #DEFINE OUR NODE STRUCTURE
    def __init__(self,val): #attributes
        self.val = val
        self.next = None

def insertAtFront(self, val: int ) -> None:
    """"
    create the new node; set its next ptr to original head and update head to point to this new node
    """"
    node = ListNode(val)
    node.next = self.head


    

